apiVersion: v1
kind: ConfigMap
metadata:
  name: wandertogether-config
  namespace: wandertogether
data:
  NODE_ENV: "production"
  # Database configuration will be handled via secrets
  # Redis/Cache configuration
  CACHE_TTL: "3600"
  # Application configuration
  MAX_UPLOAD_SIZE: "10485760"
  SESSION_TIMEOUT: "86400"